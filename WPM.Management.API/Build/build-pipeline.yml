trigger:
- master

pool:
 vmImage: 'ubuntu-latest'
 
steps:
 - script: echo Hello,world V2!
   displayName : 'Run a one-line script V2'

 - script : 
     echo Add a multiline script V2
     echo one more V2
   displayName : 'Run a multiline scrpt V2'